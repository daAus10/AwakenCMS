<%= form_with(model: review_display) do |form| %>
  <% if review_display.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(review_display.errors.count, "error") %> prohibited this review_display from being saved:</h2>

      <ul>
        <% review_display.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
