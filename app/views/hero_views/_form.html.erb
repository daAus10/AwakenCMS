<%= form_with(model: hero_view) do |form| %>
  <% if hero_view.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(hero_view.errors.count, "error") %> prohibited this hero_view from being saved:</h2>

      <ul>
        <% hero_view.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :image, style: "display: block" %>
    <%= form.file_field :image, multiple: true %>
  </div>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.label :button, style: "display: block" %>
    <%= form.text_field :button %>
  </div>

  <div>
    <%= form.label :isVisible, style: "display: block" %>
    <%= form.check_box :isVisible %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
