<%= form_with(model: service_item) do |form| %>
  <% if service_item.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(service_item.errors.count, "error") %> prohibited this service_item from being saved:</h2>

      <ul>
        <% service_item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :service_name, style: "display: block" %>
    <%= form.text_field :service_name %>
  </div>

  <div>
    <%= form.label :time, style: "display: block" %>
    <%= form.number_field :time %>
  </div>

  <div>
    <%= form.label :price, style: "display: block" %>
    <%= form.number_field :price %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_area :description %>
  </div>

  <%= form.collection_select(:service_category, ServiceCategory.all, :id, :title, prompt: "Select a category") %>
  <div>
    <%= form.submit %>
  </div>
<% end %>
