<%= form_with(model: review_view) do |form| %>
  <% if review_view.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(review_view.errors.count, "error") %> prohibited this review_view from being saved:</h2>

      <ul>
        <% review_view.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.text_field :content %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
